# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
python-dotenv>=1.0.1

# --- Base de Datos ---
pymongo>=4.9.0

# --- Google AI (CRÍTICO) ---
# Esta es la NUEVA librería que usa tu matching_service.py (Cliente V1)
google-genai>=0.3.0

# Esta es la VIEJA librería (necesaria para que LangChain funcione internamente)
google-generativeai>=0.8.3

# --- LangChain (Ecosistema 0.3.x) ---
langchain>=0.3.7
langchain-core>=0.3.15
langchain-community>=0.3.5
langchain-google-genai>=2.0.1
langchain-groq>=0.2.1

# --- Cálculo Matemático (Nuevo requerimiento) ---
# Necesario para los cálculos vectoriales (cosine similarity) en matching_service
numpy>=1.26.0

# --- Herramientas de Validación (Fuzzy Match) ---
thefuzz>=0.22.1
python-Levenshtein>=0.26.0

# --- Utilidades Opcionales ---
requests>=2.32.3
# Para procesamiento de imágenes
Pillow